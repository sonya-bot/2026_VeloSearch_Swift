import SwiftUI

struct CollectionView: View {
    var body: some View {
        VStack {
            HStack{
                Text("Collections")
                    .font(.largeTitle)
                    .bold()
                    .padding(10)
                Spacer()
            }
            Spacer()
        }
    }
}