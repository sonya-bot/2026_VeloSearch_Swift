
# AGENTS.md

このファイルは、このリポジトリで作業するAIエージェント向けの共通ルールです。
リポジトリ全体に適用します。

## 正本と優先順位

- プロジェクトの目的、設計、開発方針、完了要件は、ルートの `README.md` を正本とします。
- アプリ固有の実行方法、ビルド方法、開発環境については、リポジトリ内の関連ドキュメントを参照します。
- Issueの受け入れ条件と、対象ディレクトリ内のより具体的な指示がある場合は、それらも遵守します。
- 指示が競合する場合は、ユーザーの明示的な指示を最優先します。
- 解決できない競合がある場合は、推測で実装を進めず、作業前に確認します。

## プロジェクト構成

- アプリケーションコードは、既存のディレクトリ構成と責務分離に従って配置します。
- UIはSwiftUIまたはプロジェクトで採用されているUIフレームワークを使用し、READMEや設計資料に定めるアーキテクチャと依存方向を維持します。
- MVVMを採用している場合、View、ViewModel、Model、Repository、Serviceなどの責務を明確に分離します。
- ViewからRepository、Service、API、データベース、ファイルシステム、センサー、カメラ、その他の外部リソースを直接操作しません。
- ViewModelからView、SwiftUIの具体的なView型、UIKitのViewController、データベース、ファイルシステムを直接操作しません。
- 永続データはRepositoryなどのデータアクセス層を介して扱い、画面状態や画面遷移などのUI状態と分離します。
- OS固有機能や外部サービスへのアクセスは、適切なServiceまたは抽象化層を介して行います。
- APIキー、認証情報、秘密鍵、個人情報をコード、ログ、テストデータ、設定ファイル、リポジトリへ追加しません。
- 新しいファイルやディレクトリを追加する場合は、既存の構成と命名規則を確認し、同じ責務のコードが分散しないようにします。

## Swiftコーディング規約

- Swift API Design Guidelinesおよびプロジェクトで採用しているLint、Formatルールに従います。
- SwiftLintやSwiftFormatなどが導入されている場合は、その設定を優先します。
- 変数、引数、クロージャ引数には、その役割が分かる具体的な名前を付けます。
- `e`、`err`、`ex` のような、エラーを表す省略名や一文字名を使用しません。
- エラーは `error`、必要に応じてエラー内容を表す具体的な名前を使用します。
- エラー以外でも、意味が読み取れない一文字名や独自の省略名を使用しません。
- 型、関数、変数、プロパティの名前は、実装方法ではなく責務や目的を表すものにします。
- 1行は、インデントを含めて原則120文字以内に収めます。
- URL、自動生成コード、分割によって著しく可読性が低下する記述など、分割が不可能または不適切な場合のみ120文字超過を許容します。
- 長い式は、意味のあるローカル変数、関数、複数行の引数、クロージャ、メソッドチェーンなどへ分割します。
- コメントはコードから分かる処理の言い換えではなく、判断理由、制約、設計意図、回避できない実装上の事情を説明する場合にだけ追加します。
- 既存コードのスタイルを尊重し、依頼と無関係な整形、命名変更、リファクタリング、大規模な書き換えを混在させません。

### 真偽値の命名

- `Bool` の変数、プロパティ、引数、真偽値を返す関数は、真の状態が一目で分かる名前にします。
- 状態や性質には `is`、所有や存在には `has`、可否には `can`、判断や方針には `should` を基本として使用します。
- `visible`、`connected`、`edit` のような曖昧な名前は避け、`isVisible`、`isConnected`、`canEdit` のように命名します。
- 否定形や二重否定を避け、可能な限り肯定形で命名します。
- `Bool` を返すクロージャやpredicateにも同じ規則を適用します。
- Swift標準APIやApple Frameworkで慣例となっている表現がある場合は、その命名規則を優先します。

### 関数とクロージャの命名

- 関数名は、その呼び出しが自然な英文として読めるよう、Swift API Design Guidelinesに従って命名します。
- 関数が何を行うか、または何を返すかが名前から判断できるようにします。
- `getData`、`process`、`handle`、`doStuff` のように対象や目的が不明な名前を使用しません。
- 値を単純に取得するだけの場合、不要な `get` 接頭辞は使用しません。
- 必要に応じて `from`、`to`、`with`、`for`、`using` などを用いて、入力、変換方向、対象、条件を明確にします。
- UIイベントに対応するメソッドやクロージャは、対象とイベントが分かる名前にします。
- 例として、保存ボタン押下時の処理には `onSaveButtonTapped` や `saveButtonTapped` など、プロジェクト内で統一された命名を使用します。
- `callback`、`closure`、`function` のような、役割を示さないクロージャ名を使用しません。
- escapingクロージャをプロパティとして保持する場合も、その目的が分かる名前を使用します。

### 型とUIコンポーネントの命名

- 型名には `UpperCamelCase`、変数、プロパティ、関数、引数には `lowerCamelCase` を使用します。
- SwiftUIのViewやUIKitのView、ViewControllerなどの名前は、必要に応じて「使用場所・役割・種類」が分かる具体的な名前にします。
- `Button`、`Content`、`Item`、`View` のような汎用名だけで、プロジェクト固有のUIコンポーネントを命名しません。
- 例として、登録内容を保存するボタンには `RegistrationRecordSaveButton` のように責務が分かる名前を使用します。
- 一方で、名前を不必要に長くせず、配置場所や型の文脈から明らかな情報は重複させません。
- `Manager`、`Helper`、`Utility` など、責務が曖昧になりやすい型名は原則として避け、具体的な責務を名前に反映します。

### 引数と依存関係

- Viewやその他の型の動作、業務ロジック、必須データに関わる依存関係は、原則として明示的に注入します。
- 必須の依存関係や値をOptionalや不適切なデフォルト値にして、呼び出し側の指定漏れを隠しません。
- 空のクロージャ `{}` をデフォルト値として指定し、必要な処理の設定漏れを隠しません。
- 表示上の任意設定や、安全かつ意味が明確な初期値など、Swiftおよび採用フレームワークで自然な場合に限りデフォルト値を使用します。
- 依存関係は呼び出し側から明示し、ViewやViewModel内部で具体実装を暗黙に生成しません。
- テスト可能性を維持するため、外部サービスやOS機能への依存は必要に応じてprotocolで抽象化します。
- グローバル状態やSingletonへの新たな依存を安易に追加しません。
- 既存設計でDependency Injectionの方法が定められている場合は、その方式に従います。

### SwiftUI

- SwiftUIを使用する場合、Viewは表示とユーザー操作の宣言に集中させます。
- View内へ業務ロジック、データ永続化、ネットワーク処理、複雑なデータ変換を実装しません。
- `body` が過度に肥大化する場合は、意味のある単位でViewまたはprivateなViewBuilderへ分割します。
- 単に行数を減らすためだけの過剰なView分割は行いません。
- `@State`、`@Binding`、`@StateObject`、`@ObservedObject`、`@Environment` などは、それぞれの所有権とライフサイクルを考慮して使用します。
- ViewModelの所有権をViewが持つ場合と、外部から注入される場合を区別します。
- UI表示のためだけの一時状態と、アプリケーションの業務状態を混在させません。
- `Task` や非同期処理をViewへ直接記述する場合も、Viewの責務を超える処理はViewModelやServiceへ移します。

### UIKit

- UIKitを使用する場合、ViewControllerへ業務ロジックを集中させません。
- ViewControllerは画面ライフサイクル、Viewの構成、ユーザー操作の受け渡しを中心に扱います。
- データ取得、永続化、外部サービスとの通信などは適切な層へ分離します。
- Storyboard、XIB、コードベースUIのいずれを使用する場合も、プロジェクトで既に採用されている方式を尊重します。
- 新しいUI実装方式を、明確な理由なく既存方式へ混在させません。

### Concurrency

- 非同期処理は、原則としてSwift Concurrencyの `async` / `await` を使用します。
- プロジェクトで既存の非同期処理方式が定められている場合は、その方針との整合性を維持します。
- UI更新が必要な処理はMainActor上で行います。
- ViewModel全体がUI状態を管理する場合は、必要に応じて `@MainActor` を付与します。
- `Task.detached` は、実行コンテキストを意図的に切り離す必要がある場合のみ使用します。
- 不要な `DispatchQueue.main.async` やGCDとSwift Concurrencyの混在を避けます。
- 非同期処理のキャンセル可能性を考慮し、画面破棄後も不要な処理が継続しないようにします。
- `try?` によって重要なエラーを黙って破棄しません。

### Optional

- 強制アンラップ `!` は原則として使用しません。
- `if let`、`guard let`、Optional chaining、Nil-Coalescingなどを適切に使用します。
- 値が存在しないことが正常な状態でない場合は、Optionalにすることで問題を隠さず、型設計を見直します。
- `as!` による強制キャストは原則として使用しません。
- IBOutletなど、フレームワーク上やむを得ないImplicitly Unwrapped Optionalについては、その利用範囲を限定します。

### エラー処理

- 回復可能な失敗は、Swiftの `throws`、`Result`、またはプロジェクトで定められたエラー表現を使用して明示的に扱います。
- 意味のあるエラー型を定義し、呼び出し側が必要に応じて原因を判断できるようにします。
- `try!` は原則として使用しません。
- `try?` はエラーの詳細が不要で、失敗を `nil` として扱うことが仕様上明確な場合にのみ使用します。
- catchしたエラーを理由なく無視しません。
- ユーザー向けメッセージと内部エラー情報を分離し、内部実装や機密情報をそのままUIへ表示しません。

### アクセス制御

- 型、プロパティ、関数のアクセスレベルは必要最小限にします。
- 外部公開する必要がないものは `private` または `fileprivate` を検討します。
- モジュール外へ公開する必要がないAPIを不用意に `public` または `open` にしません。
- テストのためだけに、本来非公開である実装詳細を公開しません。

### ログ、未使用コード、import

- 一時的な `print`、デバッグ出力、検証用ログを理由なく完成コードへ残しません。
- 継続して必要なログには、プロジェクトで採用しているLogging APIを使用します。
- Appleプラットフォームでは、必要に応じて `Logger` などの適切なAPIを使用します。
- ログには目的上必要な情報だけを含め、APIキー、認証情報、個人情報、その他の機密情報を出力しません。
- 未使用の変数、引数、関数、型、import、到達不能コードは削除します。
- 使用しなくなったコメントアウト済みコードを完成コードへ残しません。
- 型やライブラリは通常のimportで参照し、不要なラッパーやaliasによって可読性を下げません。

### メモリ管理

- クロージャによる循環参照を発生させないよう、キャプチャ関係を確認します。
- 必要に応じて `[weak self]` または `[unowned self]` を使用します。
- `[unowned self]` は、クロージャ実行時に対象が必ず存在することが保証される場合にのみ使用します。
- `weak` を機械的に付与せず、オブジェクトの所有関係を確認して判断します。
- Delegateを保持する場合は、所有関係に応じて `weak` を使用します。

## テスト

- 変更した責務に対応する既存テストがある場合は、必要に応じて更新します。
- 新しい業務ロジックや不具合修正には、合理的な範囲でテストを追加します。
- Viewの細かな実装詳細ではなく、外部から観測できる振る舞いを中心にテストします。
- テストコードでも本番コードと同じ命名規則と可読性基準を適用します。
- テストを通すためだけに、本番コードの設計やアクセス制御を不自然に変更しません。
- 実行可能なテスト、Lint、ビルド確認を行い、変更による既存機能への影響を確認します。

## 変更時の原則

- 依頼された範囲を超える変更を行いません。
- 既存コードを変更する前に、関連する実装、テスト、設計、呼び出し元を確認します。
- 不具合修正では、表面的な症状だけでなく原因を特定したうえで修正します。
- 新機能の実装では、既存のアーキテクチャと責務分離を維持します。
- 既存APIや公開インターフェースを変更する場合は、影響範囲を確認します。
- 不要な依存パッケージや外部ライブラリを追加しません。
- 標準ライブラリや既存依存関係で十分に実現できる場合は、それらを優先します。
- 自動生成ファイルは、明示的に必要な場合を除き直接編集しません。
- 生成元を変更すべきファイルについては、生成元を修正して再生成します。

## 完了前の確認

作業完了前に、少なくとも以下を確認します。

- 要求された変更がすべて実装されていること。
- README、Issue、設計資料、対象ディレクトリの指示に違反していないこと。
- アーキテクチャ上の責務と依存方向を崩していないこと。
- Swiftの命名規則およびプロジェクトのLintルールを満たしていること。
- 不要なデバッグコード、未使用コード、未使用importが残っていないこと。
- 機密情報や個人情報を追加していないこと。
- 変更可能な範囲でビルド、テスト、Lintを実行していること。
- 依頼と無関係な変更が含まれていないこと。
